import { __assign } from "tslib";
import { useMemo } from 'react';
export var useCarousel = function (_a) {
    var selected = _a.selected, cards = _a.cards, currentPage = _a.currentPage, isSideCardsShown = _a.isSideCardsShown;
    var totalCount = cards.length;
    var selectedCards = cards.filter(function (_el, index) {
        if (currentPage !== 1 && isSideCardsShown) {
            return (currentPage - 1) * selected <= index + currentPage && index + currentPage - 1 < currentPage * selected;
        }
        return (currentPage - 1) * selected <= index && index < currentPage * selected;
    });
    var paginationRange = useMemo(function () {
        var totalPageCount = totalCount / selected;
        if (isSideCardsShown) {
            totalPageCount = totalCount / (selected - 1);
        }
        var totalPageCountRound = Math.ceil(totalPageCount);
        return { totalPageCount: totalPageCountRound };
    }, [totalCount, selected, isSideCardsShown]);
    return __assign(__assign({}, paginationRange), { selectedCards: selectedCards });
};
//# sourceMappingURL=useCarusel.js.map