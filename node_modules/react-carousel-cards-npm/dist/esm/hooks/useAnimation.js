import { useEffect, useState } from 'react';
export var useAnimation = function (_a) {
    var withAnimation = _a.withAnimation, selectedCards = _a.selectedCards;
    var _b = useState(false), isAnimate = _b[0], setIsAnimate = _b[1];
    var disableAnimation = function () { return setIsAnimate(false); };
    useEffect(function () {
        var _a;
        if (!withAnimation || isAnimate) {
            return;
        }
        var sliderInterval = setInterval(function () {
            setIsAnimate(true);
        }, (_a = withAnimation.animationInterval) !== null && _a !== void 0 ? _a : 500);
        return function () {
            clearInterval(sliderInterval);
        };
    }, [withAnimation, isAnimate]);
    useEffect(function () {
        if (!withAnimation) {
            return;
        }
        disableAnimation();
    }, [JSON.stringify(selectedCards)]);
    return { isAnimate: isAnimate, disableAnimation: disableAnimation };
};
//# sourceMappingURL=useAnimation.js.map