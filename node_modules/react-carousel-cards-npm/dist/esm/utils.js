import React from 'react';
export var renderChildren = function (children, props) {
    return React.Children.map(children, function (child) {
        if (React.isValidElement(child)) {
            return React.cloneElement(child, props);
        }
        return child;
    });
};
export var renderChild = function (child, props) {
    if (React.isValidElement(child)) {
        return React.cloneElement(child, props);
    }
    return child;
};
export var setNextPage = function (callback) { return function () {
    return callback(function (prevState) { return prevState + 1; });
}; };
export var setPrevPage = function (callback) { return function () {
    return callback(function (prevState) { return (prevState < 2 ? 1 : prevState - 1); });
}; };
//# sourceMappingURL=utils.js.map