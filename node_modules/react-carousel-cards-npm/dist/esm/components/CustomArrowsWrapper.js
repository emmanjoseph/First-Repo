import React from 'react';
import { renderChild } from '../utils';
import HeaderArrows from './HeaderArrows';
var CustomArrowsWrapper = function (_a) {
    var CustomArrowBtn = _a.CustomArrowBtn, disabled = _a.disabled, currentPage = _a.currentPage, lastPage = _a.lastPage, handlePrevPage = _a.handlePrevPage, handleNextPage = _a.handleNextPage;
    return (React.createElement("div", null, CustomArrowBtn ? (React.createElement(React.Fragment, null,
        renderChild(CustomArrowBtn, {
            disabled: disabled || currentPage === 1,
            onClick: handlePrevPage,
            isLeftArrow: true,
        }),
        renderChild(CustomArrowBtn, {
            disabled: disabled || currentPage === lastPage,
            onClick: handleNextPage,
            isLeftArrow: false,
        }))) : (React.createElement(HeaderArrows, { disabled: disabled, lastPage: lastPage, currentPage: currentPage, goToNextPage: handleNextPage, goToPrevPage: handlePrevPage }))));
};
export default CustomArrowsWrapper;
//# sourceMappingURL=CustomArrowsWrapper.js.map