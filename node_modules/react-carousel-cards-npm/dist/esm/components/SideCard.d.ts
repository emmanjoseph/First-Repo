/// <reference types="react" />
import { ISideCard } from '../types/CarouselTypes';
declare const SideCard: ({ child, cardWidth, item, width, index, selected, widthCard, marginCard }: ISideCard) => JSX.Element;
export default SideCard;
